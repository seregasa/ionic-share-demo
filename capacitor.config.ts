import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-share-demo',
  webDir: 'www',
  ios: {
    backgroundColor: '#ffffff'
  }
};

export default config;
